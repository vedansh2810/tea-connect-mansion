node_modules
dist
dist-ssr
.DS_Store
.vite

# Local configuration. Never commit secrets.
# VITE_* vars are embedded in the JS bundle — they are NOT secret.
# The Supabase service_role key must NEVER appear here or in the code.
.env
.env.local
.env.production
.env.*.local
*.local.bak

# Working files from transcribing the printed menu
_pdfpages/
*.pdf
